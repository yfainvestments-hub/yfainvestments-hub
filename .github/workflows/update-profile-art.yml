name: Update profile art

on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-profile-art
  cancel-in-progress: true

jobs:
  heatmap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install generator dependencies
        run: pip install -r scripts/requirements.txt
      - name: Refresh contribution data and artwork
        env:
          # Optional PAT with read:user. When set, contributions come from the
          # GraphQL API and include private repos regardless of the profile's
          # "Include private contributions" setting. Without it the job scrapes
          # the public profile page, which only shows private counts while that
          # setting is enabled.
          GH_CONTRIB_TOKEN: ${{ secrets.GH_CONTRIB_TOKEN }}
        run: |
          python scripts/fetch_contributions.py
          python scripts/render_heatmap_svg.py
          python scripts/make_identity_svg.py
          python scripts/make_info_card.py
      - name: Commit refreshed artwork
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "chore: refresh profile artwork [skip ci]"
          file_pattern: "data/contributions.json contrib-heatmap.svg identity-ascii.svg info-card.svg assets/avatar.png"
